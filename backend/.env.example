# Database
DATABASE_URL=mysql://root:root@127.0.0.1:3306/posi_db

# Auth
JWT_SECRET=change-me
JWT_EXPIRES=30d

# Firebase (optional, needed for push notif)
FIREBASE_SERVICE_ACCOUNT=./service-account.json

# Server
PORT=4000
# Comma-separated; '*' allows all (use only in dev). Example for web debug ports:
# CORS_ORIGIN=http://localhost:*,http://127.0.0.1:*,http://10.0.2.2:*
CORS_ORIGIN=*
